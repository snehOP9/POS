# GitHub Publish Plan

1. Confirm the local frontend, API, database connectivity, and seeded-login
   smoke checks.
2. Refresh the root README with the current frontend/backend structure,
   capabilities, prerequisites, setup, scripts, Docker usage, and safety
   notes.
3. Initialize this workspace as a Git repository, keep `.env`, dependencies,
   build output, and local uploads excluded, then commit the application,
   documentation, configuration, and lockfile.
4. Push the verified initial commit to `https://github.com/snehOP9/POS.git`.
